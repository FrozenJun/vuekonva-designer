import { BASE_WIDTH, BASE_HEIGHT } from './variable.constant';

// 默认的content值
export const DEFAULT_STAGE_CONTENT = {
  canvas: {
    width: BASE_WIDTH,
    height: BASE_HEIGHT,
    background: '#FFF'
  },
  shapeList: []
};
